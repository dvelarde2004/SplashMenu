<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="#1A1A2E"
    android:padding="30dp">

    <!-- Título -->
    <TextView
        android:id="@+id/texto_titulo_play_games"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="PLAY GAMES"
        android:textSize="36sp"
        android:textStyle="bold"
        android:textColor="#FFD700"
        android:layout_marginBottom="50dp" />

    <!-- Botón Snake Game -->
    <Button
        android:id="@+id/boton_snake_game"
        android:layout_width="250dp"
        android:layout_height="65dp"
        android:text="SNAKE GAME"
        android:textSize="18sp"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:background="@drawable/boton_menu_selector"
        android:layout_marginBottom="25dp" />

    <!-- Botón 2000 Game -->
    <Button
        android:id="@+id/boton_2000_game"
        android:layout_width="250dp"
        android:layout_height="65dp"
        android:text="2000 GAME"
        android:textSize="18sp"
        android:textColor="#CCCCCC"
        android:textStyle="bold"
        android:background="@drawable/boton_menu_selector"
        android:layout_marginBottom="50dp" />

    <!-- Botón Volver -->
    <Button
        android:id="@+id/boton_volver_play_games"
        android:layout_width="200dp"
        android:layout_height="55dp"
        android:text="BACK"
        android:textSize="16sp"
        android:textColor="#CCCCCC"
        android:background="@drawable/boton_menu_selector" />

</LinearLayout>